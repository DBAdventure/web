<template>
    <img data-trigger="hover"
         data-toggle="tooltip"
         data-placement="top"
         title="({{ x }}, {{ y }}) {{ name }}"
         src="{{ asset(imagePath) }}"
    />
</template>
